import 'package:flutter/widgets.dart';

class TerraSheet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 100,
      height: 100,
      child: ColoredBox(
        color: Color(0xfff0eee7),
      ),
    );
  }
}
